# Committed baseline screenshots for visual regression.
#
# To update baselines:
#   1. Download the ui-shots-desktop artifact from the latest CI run
#   2. Copy the .png files into this directory
#   3. Commit and push
#
# The CI visual regression step compares new screenshots against these baselines.
# If no baselines exist, the check is skipped (not failed).
